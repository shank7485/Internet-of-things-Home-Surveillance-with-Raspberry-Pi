On Pi:

Place all zipped files for Raspberry Pi on Desktop and run thr following from command line. 
Bash Commands:
cd ~/Desktop/
sudo bash CameraandGPIO
sudo JavaClientandSSH

On Server:

Place all zipped files for server, except pagekite.py on Desktop and place pagekite.py in the ~/Downloads folder. This is because the bash scripts work in pre specified directories. 

Bash Commands:
cd ~/Desktop
bash Complete



